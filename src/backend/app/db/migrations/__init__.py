"""
Database migrations package for IndiVillage.com application.

This package contains database migration scripts managed by Alembic.
Migrations enable version-controlled database schema changes that can be
applied sequentially and tested automatically.

Version history is tracked to ensure proper migration sequencing and
to support rollback capabilities if needed.
"""

__version__ = "0.1.0"